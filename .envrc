# Obsidian Palace MCP - Development Environment
# This file is used by direnv to set environment variables for development
# See: https://direnv.net/

# Path to the Obsidian vault used for development and testing
export PALACE_VAULT_PATH="/Users/adamc/Documents/Claude Palace"

# Log level (debug, info, warn, error)
export PALACE_LOG_LEVEL="debug"

# Enable file watcher for external changes
export PALACE_WATCH_ENABLED="true"

# Optional: Custom index location (defaults to {vault}/.palace/index.sqlite)
# export PALACE_INDEX_PATH="/custom/path/to/index.sqlite"
